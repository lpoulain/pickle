// This is the class where you define all your Pickle classes.
// You can hard-code their values, store them in the database,
// go through the ApexClass object for a given naming convention, etc.
public virtual class PickleAllScenarios {
    public List<String> getPickles() {
        return new String[] { 'CustomPickle' };
    }
}